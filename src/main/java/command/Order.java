package command;

/**
 * define Order(actually command) interface, which gives an interface to operator to execute different given command
 * @author 1853615邓斯语
 */
public interface Order {
    /**
     * execute
     */
    void execute();
}
